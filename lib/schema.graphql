type EnVivo{
    title: String
    description: String
    canonicalUri:String
    
}
type Query {
    "Devuelve todos los videos en vivo"
    getEnVivo(uid: String, pagina: String, ui: String): [EnVivo]
}